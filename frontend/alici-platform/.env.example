NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Set to true to disable authentication enforcement in development.
# The root route (/) will redirect directly to /dashboard.
# NEVER enable this in production.
NEXT_PUBLIC_DEV_MODE=true
